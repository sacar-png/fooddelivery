info:
  name: Fetch Orders
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:9097/api/orders
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
